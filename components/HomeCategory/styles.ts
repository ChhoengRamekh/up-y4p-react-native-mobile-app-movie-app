import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    image: {
        width: 100,
        height: 170,
        resizeMode: 'cover',
        borderRadius: 10,
        margin: 5
    },
    title: {
        fontSize: 24,
        fontWeight: 'bold'
    }
})

export default styles